{% extends 'default_frame.twig' %}

{% block title 'EFO設定' %}

{% block main %}
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <form class="form-horizontal" action="{{ url('plugin_Efo_config') }}" method="POST" {{ form_enctype(form) }}>
        <input type="hidden" name="_method" value="PUT">
        {{ form_widget(form._token) }}

        <div class="form-group">
          <label class="control-label col-sm-12">注文フォーム最適化</label>
          <div class="col-sm-12">
            <a class="btn btn-default" href="{{ url('plugin_Efo_admin_entry_form_index') }}">管理画面へ</a>
          </div>
        </div>

        <div class="form-group">
          <label class="control-label col-sm-12">会員登録フォーム最適化</label>
          <div class="col-sm-12">
            <a class="btn btn-default" href="{{ url('plugin_Efo_admin_customer_entry_form_index') }}">管理画面へ</a>
          </div>
        </div>

        {#
        <div class="form-group">
          <div class="col-sm-8 col-sm-offset-4">
            <button type="submit" class="btn btn-primary btn-block">設定を保存</button>
          </div>
        </div>
        #}
      </form>
    </div>
  </div>
{% endblock main %}
